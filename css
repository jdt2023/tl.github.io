/* 确保样式应用到 textarea 元素 */
textarea {
    width: 100%;  /* 设置宽度为 100% */
    height: 200px;  /* 设置一个合适的高度 */
    padding: 10px;  /* 增加内边距 */
    font-size: 16px;  /* 调整字体大小 */
    box-sizing: border-box;  /* 确保内边距和边框不会增加总宽度 */
}

/* 可选：调整按钮样式 */
button {
    padding: 10px 20px;
    font-size: 16px;
    margin-top: 10px;
}
